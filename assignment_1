answers
1. values : 'he;llo', -87.8, 6
   expressions : -, / +
   
2. A Variable is a store of information, and a String is a type of information you would store in a Variable. 

3. int. float, bool, complex

4.An expression is a combination of operators and operands that is interpreted to produce some other value
5. Expressions are unique in meaning, while statements are two-sided in execution.

6. 23

7. 'spamspamspam' , 'spamspamspam'

8. Because variable names cannot begin with a number.

9. The int() , float() , and str( ) functions will evaluate to the integer, floating-point number, and string versions of the value passed to them.

10. can only concatenate str (not "int") to str. we can fix it by making it str i.e 'I have eaten' +'99'+'burritos.

   
